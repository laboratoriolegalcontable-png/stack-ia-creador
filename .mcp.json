{
  "mcpServers": {
    "apify": {
      "type": "http",
      "url": "https://mcp.apify.com/sse",
      "headers": {
        "Authorization": "Bearer ${APIFY_TOKEN}"
      }
    },
    "firecrawl": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"]
    }
  }
}
